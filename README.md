# Telegram Bot USN - Система регистрации на соревнования

Полнофункциональный Telegram бот для регистрации пользователей на соревнования USN с использованием современной архитектуры и паттернов.

## 📋 Возможности

- ✅ **Главное меню** с двумя основными опциями
- ✅ **Связь с командой** - возможность отправить сообщение команде
- ✅ **Регистрация на соревнования**:
  - Проверка активных соревнований
  - Выбор соревнования (если несколько)
  - Выбор роли в зависимости от типа соревнования
  - Создание нового профиля пользователя
  - Редактирование данных существующего пользователя
  - Валидация всех входных данных
- ✅ **Управление ролями**:
  - Игрок (Player)
  - Секундант (для Classic Game)
  - Зритель
  - Судья (Voter)
- ✅ **Управление состоянием** с использованием FSM
- ✅ **База данных** с SQLAlchemy и SQLite
- ✅ **Django админка** для управления данными

## 🏗️ Структура проекта

```
telegram_bot/
├── admin_panel/                     # Django админ-панель
│   ├── apps/BotDataApp/            # Основное приложение
│   │   ├── migrations/             # Миграции БД
│   │   ├── templates/              # HTML шаблоны
│   │   ├── admin.py                # Конфигурация Django админа
│   │   ├── models.py               # Django модели
│   │   ├── views.py                # Views для админки
│   │   └── urls.py                 # URL маршруты
│   ├── manage.py                   # Команда управления Django
│   ├── settings.py                 # Конфигурация Django
│   ├── urls.py                     # Основные URL маршруты
│   └── wsgi.py                     # WSGI приложение
├── config/                          # Конфигурация бота
│   ├── settings.py                 # Основные настройки
│   └── __init__.py
├── states/                          # FSM состояния
│   ├── registration.py             # Состояния регистрации
│   ├── contact.py                  # Состояния контакта
│   └── __init__.py
├── handlers/                        # Обработчики команд
│   ├── start.py                    # Команда /start
│   ├── menu.py                     # Главное меню
│   ├── contact.py                  # Контакт с командой
│   ├── registration/               # Обработчики регистрации
│   │   ├── competition_select.py   # Выбор соревнования
│   │   ├── role_select.py          # Выбор роли
│   │   ├── user_create.py          # Создание пользователя
│   │   ├── user_edit.py            # Редактирование пользователя
│   │   ├── confirmation.py         # Финальное подтверждение
│   │   └── __init__.py
│   └── __init__.py
├── messages/                        # Текстовые сообщения
│   ├── texts.py                    # Класс с текстами
│   └── __init__.py
├── keyboards/                       # Клавиатуры
│   ├── inline.py                   # Инлайн-кнопки
│   ├── reply.py                    # Reply-кнопки
│   └── __init__.py
├── models/                          # Модели данных
│   ├── user.py                     # Модель пользователя
│   ├── competition.py              # Модель соревнования
│   ├── registration.py             # Модель регистрации
│   └── __init__.py
├── utils/                           # Утилиты
│   ├── database.py                 # Операции с БД
│   ├── validators.py               # Валидаторы
│   ├── helpers.py                  # Вспомогательные функции
│   └── __init__.py
├── bot.py                          # Инициализация бота
├── main.py                         # Точка входа
├── requirements.txt                # Зависимости
├── .env.example                    # Пример переменных окружения
├── ADMIN_PANEL_SETUP.md            # Документация админ-панели
└── README.md                       # Этот файл
```

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
cd telegram_bot
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Скопируйте `.env.example` в `.env` и заполните необходимые значения:

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
BOT_TOKEN=YOUR_ACTUAL_BOT_TOKEN
DATABASE_URL=sqlite+aiosqlite:///./bot_database.db
DEBUG=True
LOGGING_LEVEL=INFO
ADMIN_IDS=123456789
```

### 3. Запуск бота

```bash
python main.py
```

### 4. Запуск Django админ-панели (опционально)

Админ-панель позволяет управлять данными через веб-интерфейс:

```bash
./start-admin-panel.sh
```

Или вручную:

```bash
cd admin_panel
python manage.py migrate
python manage.py createsuperuser  # Создать администратора
python manage.py runserver
```

Откройте в браузере: http://localhost:8000/admin

> 📖 Подробнее см. в [ADMIN_PANEL_SETUP.md](ADMIN_PANEL_SETUP.md)

## 📐 Архитектура

### Технологический стек

- **aiogram 3.4.1** - асинхронная библиотека для работы с Telegram Bot API
- **SQLAlchemy 2.0** - ORM для работы с БД
- **Django 4.2** - веб-фреймворк для админ-панели
- **SQLite** - легковесная БД по умолчанию
- **python-dotenv** - загрузка переменных окружения

### Паттерны и принципы

- **FSM (Finite State Machine)** - управление состоянием диалога
- **Модульная архитектура** - разделение функциональности на независимые модули
- **Class-Based подход** - использование классов для организации кода
- **Async/Await** - асинхронное программирование
- **Валидация данных** - проверка всех входных данных
- **Разделение ответственности** - каждый модуль отвечает за свою задачу

## 🔄 Процесс регистрации

```
[Главное меню]
    ↓
[Выбор: Контакт / Регистрация]
    ↓
[Регистрация]
    ├─→ [Проверка активных соревнований]
    │   ├─→ Нет → [Сообщение об отсутствии]
    │   └─→ Да ↓
    │
    ├─→ [Выбор соревнования (если >1)]
    │   └─→ ↓
    │
    ├─→ [Выбор роли]
    │   └─→ ↓
    │
    ├─→ [Проверка пользователя]
    │   ├─→ Существует ↓
    │   │   ├─→ [Показать данные]
    │   │   ├─→ Да → [Регистрация успех]
    │   │   └─→ Нет → [Редактирование данных]
    │   │       └─→ [Повторная проверка]
    │   │
    │   └─→ Новый ↓
    │       ├─→ [Сбор данных пользователя]
    │       │   (Имя, фамилия, телефон, email, страна, город, клуб)
    │       │
    │       ├─→ [Вопрос: Player/Voter?]
    │       │   ├─→ Да → [Сертификат, Компания, Должность]
    │       │   │         → [Повторный вопрос: Player/Voter?]
    │       │   │           ├─→ Да → [Представление] → [Создание]
    │       │   │           └─→ Нет → [Создание]
    │       │   └─→ Нет → [Компания, Должность]
    │       │            → [Повторный вопрос: Player/Voter?]
    │       │              ├─→ Да → [Сертификат, Представление] → [Создание]
    │       │              └─→ Нет → [Создание]
    │       │
    │       └─→ [Финальная проверка данных]
    │           ├─→ Да → [Регистрация успех]
    │           └─→ Нет → [Редактирование]
    │
    └─→ [Возврат в главное меню]
```

## 🔐 Валидация данных

- **Телефон**: `+7/8 + 10 цифр`
- **Email**: стандартный email формат
- **Имя/Фамилия**: 2-50 символов
- **Имя для сертификата**: латиница, 3-100 символов
- **Прочие поля**: 2-255 символов

## 📊 Модель данных

### UserModel
- `telegram_id` - уникальный ID пользователя в Telegram
- `telegram_username` - username Telegram (опционально)
- `first_name`, `last_name` - имя и фамилия
- `phone` - уникальный номер телефона
- `email` - уникальный email
- `country`, `city` - географическая информация
- `club` - представляемый клуб/школу
- `company`, `position` - информация о работе
- `certificate_name` - имя для сертификата (для Player/Voter)
- `presentation` - представление на соревнованиях (для Player/Voter)

### CompetitionModel
- `name` - название соревнования
- `competition_type` - тип (classic_game, puzzle, etc.)
- `available_roles` - доступные роли (JSON)
- `is_active` - активно ли соревнование

### RegistrationModel
- `user_id` - ID пользователя
- `competition_id` - ID соревнования
- `role` - выбранная роль
- `is_confirmed` - подтверждена ли регистрация

## 🛠️ Расширение функциональности

### Добавление новой роли

1. Обновить `BotMessages`:
```python
ROLE_NEW = "Новая роль"
```

2. Обновить логику в `BotHelpers.get_available_roles_for_competition()`:
```python
if competition_type.lower() == "specific_type":
    base_roles.append("new_role")
```

### Добавление нового поля пользователя

1. Обновить `UserModel`:
```python
new_field = Column(String(255), nullable=True)
```

2. Обновить `BotMessages`:
```python
REQUEST_NEW_FIELD = "Запрос нового поля:"
```

3. Добавить валидатор в `Validators`
4. Добавить обработчик в `user_create.py`
5. Добавить поле в `edit_fields_for_role()`

## 📝 Логирование

Логи записываются в консоль с форматом:
```
2024-01-01 12:00:00,000 - bot_module - INFO - Сообщение логирования
```

Уровень логирования настраивается в `.env`:
- `DEBUG` - детальная информация
- `INFO` - обычная информация
- `WARNING` - предупреждения
- `ERROR` - ошибки
- `CRITICAL` - критические ошибки

## 🎨 Архитектурные принципы

Проект разработан с использованием проверенных паттернов:

- ✅ **Модульная структура** - четкое разделение функциональности
- ✅ **Класс-based approach** - использование классов для организации
- ✅ **Разделение ответственности** - handlers, models, utils, messages
- ✅ **Конфигурация** - отдельный модуль config для настроек
- ✅ **Валидация** - проверка всех входных данных
- ✅ **Управление состоянием** - FSM для контроля диалогов

## 🌐 Django Админ-панель

Проект включает встроенную Django админ-панель для управления данными:

- 📊 **Dashboard** - статистика по пользователям и регистрациям
- 👥 **Управление пользователями** - список и редактирование
- 📋 **Управление регистрациями** - обработка заявок
- 🏆 **Управление соревнованиями** - настройка событий

**Быстрый старт**:
```bash
./start-admin-panel.sh
# Откройте http://localhost:8000
```

Подробнее: [QUICK_ADMIN_START.md](QUICK_ADMIN_START.md)

## 📄 Лицензия

Проект разработан как самостоятельное решение для регистрации на соревнования USN.

## 💬 Поддержка

Для вопросов и предложений свяжитесь с командой разработки.# telegram_bot
