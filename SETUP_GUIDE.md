# 🚀 Руководство по настройке Telegram бота USN

Это руководство поможет вам настроить бот и админ-панель под ваши нужды.

---

## 📋 Содержание

1. [Требования](#требования)
2. [Быстрый старт](#быстрый-старт)
3. [Конфигурация](#конфигурация)
4. [Запуск бота](#запуск-бота)
5. [Запуск админ-панели](#запуск-админ-панели)
6. [Настройка под себя](#настройка-под-себя)
7. [Решение проблем](#решение-проблем)

---

## 📦 Требования

- Docker и Docker Compose (рекомендуется) **ИЛИ**
- Python 3.10+
- PostgreSQL (опционально, вместо SQLite)

---

## ⚡ Быстрый старт

### Вариант 1: Docker (Рекомендуется)

```bash
# Клонируйте проект
git clone <ваш-репозиторий> telegram_bot
cd telegram_bot

# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env (добавьте BOT_TOKEN)
nano .env

# Запустите бота
make up

# Просмотрите логи
make logs
```

### Вариант 2: Локально (Python)

```bash
cd telegram_bot

# Установите зависимости
pip install -r requirements.txt

# Скопируйте конфигурацию
cp .env.example .env

# Отредактируйте .env
nano .env

# Запустите бота
python main.py
```

---

## 🔧 Конфигурация

### 1. Создайте Telegram бота

1. Откройте чат с [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте **BOT_TOKEN** (выглядит как `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### 2. Отредактируйте `.env` файл

```bash
# Обязательные параметры
BOT_TOKEN=<ваш_токен_от_BotFather>
ADMIN_IDS=<ваш_telegram_id>,<другой_админ_id>  # Через запятую

# Опциональные параметры
DEBUG=False
LOGGING_LEVEL=INFO
DATABASE_URL=sqlite+aiosqlite:///./bot_database.db

# Email для уведомлений (опционально)
MESSAGE_CONTACT_EMAIL=your-email@gmail.com
MESSAGE_CONTACT_PHONE=+7XXXXXXXXXX
```

### 3. Найдите ваш Telegram ID

1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ваш ID
3. Добавьте его в `ADMIN_IDS` в `.env`

---

## 🤖 Запуск бота

### С помощью Docker (рекомендуется)

```bash
# Запустить в фоне
make up

# Просмотреть логи в реальном времени
make logs

# Перезапустить бот
make restart

# Остановить
make down

# Полная справка по командам
make help
```

### Без Docker

```bash
python main.py
```

**Проверьте, что бот работает:**
- Откройте Telegram
- Найдите вашего бота по имени
- Отправьте команду `/start`
- Должно появиться главное меню

---

## 👨‍💼 Запуск админ-панели

### С помощью Docker

```bash
# Запустить админ-панель на порте 8000
make admin-up

# Просмотреть логи админ-панели
make admin-logs

# Остановить админ-панель
make admin-down
```

### Без Docker

```bash
cd admin_panel

# Создать администратора (если впервые)
python manage.py createsuperuser
# username: admin
# password: admin (или выберите свой)

# Запустить сервер
python manage.py runserver 0.0.0.0:8000
```

### Доступ к админ-панели

- **URL:** http://localhost:8000/admin
- **Логин:** admin
- **Пароль:** admin (измените в production!)

**Что можно делать в админ-панели:**
- 👥 Управлять пользователями
- 🏆 Создавать и редактировать соревнования
- 📋 Просматривать и одобрять регистрации
- ⏰ Управлять временными слотами для судей
- 📢 Отправлять рассылки пользователям

---

## 🎨 Настройка под себя

### 1. Изменение текстов и сообщений

**Файл:** `messages/texts.py`

```python
class BotMessages:
    GREETING = "Добро пожаловать в регистрацию!"
    BUTTON_REGISTER = "Зарегистрироваться"
    # ... и другие сообщения
```

### 2. Добавление нового соревнования

**Через админ-панель:**
1. Откройте http://localhost:8000/admin
2. Перейдите в "Competitions"
3. Нажмите "Add Competition"
4. Заполните поля:
   - Name: Название соревнования
   - Type: Тип (classic_game, puzzle и т.д.)
   - Is Active: Отметьте, чтобы сделать видимым в боте

**Или через БД напрямую:**
```bash
# Подключитесь к БД
sqlite3 bot_database.db

# Вставьте соревнование
INSERT INTO competitions (name, competition_type, is_active)
VALUES ('Мое соревнование', 'classic_game', 1);
```

### 3. Добавление администраторов

Отредактируйте `.env`:
```bash
ADMIN_IDS=123456789,987654321,555666777
```

Администраторы получат доступ к команде `/admin` в боте.

### 4. Включение почтовых уведомлений

**Файл:** `.env`

```bash
# Gmail конфигурация
MESSAGE_CONTACT_EMAIL=your-email@gmail.com

# В Gmail нужно создать "пароль приложения"
# https://myaccount.google.com/apppasswords
```

### 5. Переход на PostgreSQL (Production)

**Для больших объемов данных и масштабирования:**

```bash
# 1. Измените .env
DATABASE_URL=postgresql+asyncpg://usn_bot:password@localhost:5432/usn_bot_db

# 2. Запустите PostgreSQL
make postgres

# 3. Проверьте, что работает
make health
```

Подробнее: [POSTGRESQL_MIGRATION.md](POSTGRESQL_MIGRATION.md)

---

## 📁 Структура проекта

```
telegram_bot/
├── config/                    # Конфигурация бота
│   └── settings.py           # BOT_TOKEN, DATABASE_URL, etc.
├── models/                    # Модели БД
│   ├── user.py               # Пользователи
│   ├── competition.py        # Соревнования
│   └── registration.py       # Регистрации
├── handlers/                  # Обработчики команд
│   ├── start.py              # /start команда
│   ├── menu.py               # Главное меню
│   └── registration/         # Процесс регистрации
├── messages/                  # Текстовые сообщения
│   └── texts.py              # Измените тексты здесь
├── keyboards/                # Кнопки и клавиатуры
│   └── inline.py             # Инлайн-кнопки
├── admin_panel/              # Django админ-панель
│   ├── manage.py
│   ├── settings.py           # Django конфигурация
│   └── apps/BotDataApp/
│       ├── admin.py          # Интерфейсы админ-панели
│       └── models.py         # Django модели
├── .env.example              # Пример конфигурации
├── docker-compose.yml        # Docker конфигурация
├── Makefile                  # Удобные команды (make help)
└── requirements.txt          # Python зависимости
```

---

## 🛠️ Полезные команды

```bash
# Справка по всем доступным командам
make help

# Запустить бота в разработке (SQLite)
make dev

# Запустить с PostgreSQL
make postgres

# Просмотреть логи бота
make logs

# Просмотреть последние 50 строк логов
make logs-tail

# Статус контейнеров
make status

# Использование ресурсов
make stats

# Открыть shell в контейнере бота
make shell

# Остановить всё
make down

# Очистить Docker кэш
make prune
```

---

## 🔧 Решение проблем

### Бот не отвечает

```bash
# Проверьте логи
make logs

# Проверьте, запущен ли бот
make status

# Перезапустите
make restart

# Проверьте .env файл
cat .env
```

### "Address already in use"

```bash
# Если порт 8000 занят (для админ-панели)
# Отредактируйте .env:
ADMIN_PORT=8001

# Или найдите и завершите процесс
lsof -i :8000
kill -9 <PID>
```

### Ошибка базы данных

```bash
# Удалить старую БД и пересоздать
docker volume rm usn-bot_bot_data

# Или без Docker
rm bot_database.db

# Перезапустить
make up
```

### PostgreSQL не подключается

```bash
# Проверьте статус PostgreSQL
make status

# Проверьте конфигурацию в .env
DATABASE_URL=postgresql+asyncpg://usn_bot:secure_password@postgres:5432/usn_bot_db

# Перезапустите PostgreSQL
make db-init
```

---

## 📚 Дополнительные ресурсы

- [DOCKER_MAKEFILE_GUIDE.md](DOCKER_MAKEFILE_GUIDE.md) - Подробное руководство по Docker и Makefile
- [POSTGRESQL_MIGRATION.md](POSTGRESQL_MIGRATION.md) - Миграция на PostgreSQL
- [README.md](README.md) - Общее описание проекта

---

## ❓ Частые вопросы

### Q: Как изменить тексты в боте?
**A:** Отредактируйте `messages/texts.py`. Все сообщения в классе `BotMessages`.

### Q: Как добавить нового администратора?
**A:** Найдите его Telegram ID и добавьте в `.env` в параметр `ADMIN_IDS`.

### Q: Как переместить бота на другой сервер?
**A:** Скопируйте весь проект и файл `bot_database.db`, отредактируйте `.env`.

### Q: Нужна ли мне PostgreSQL?
**A:** Нет, SQLite работает отлично для начала. PostgreSQL нужен при большом объеме данных.

### Q: Как отправить сообщение всем пользователям?
**A:** Используйте админ-панель → Broadcasts → Create broadcast, или команду `/broadcast` для администраторов.

---

## 🎯 Что дальше?

1. ✅ Настройте `.env` с вашим BOT_TOKEN
2. ✅ Запустите бота: `make up`
3. ✅ Проверьте логи: `make logs`
4. ✅ Откройте админ-панель: `make admin-up`
5. ✅ Добавьте соревнования в админ-панели
6. ✅ Пригласите пользователей в бота

---

## 💬 Поддержка

Если у вас есть вопросы, отредактируйте этот файл и добавьте свои примеры настройки!

**Успешной работы! 🚀**
